<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns all in-app styling. This exists only so the window and
         the launch screen match the app instead of flashing platform blue. -->
    <style name="Theme.SevakAI" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/paper</item>
    </style>
</resources>
